
ASCIIDOC=@ASCIIDOC@
OPEN=@COTERMINAL_OPEN@

usermanual.html: usermanual.txt
	cat $< > ../README
	test -n "$(ASCIIDOC)" && $(ASCIIDOC) $< && $(OPEN) $@

usermanual-ja.html: usermanual-ja.txt
	cat $< > ../README
	test -n "$(ASCIIDOC)" && $(ASCIIDOC) $< && $(OPEN) $@
