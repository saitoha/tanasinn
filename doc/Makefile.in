
ASCIIDOC=@ASCIIDOC@
OPEN=@COTERMINAL_OPEN@

all: usermanual-ja.html usermanual.html

usermanual.html: usermanual.txt
	if [ -n "$(ASCIIDOC)" ]; \
	then \
		cpp -D__EN__ $< | sed -e 's/^#.*//' | $(ASCIIDOC) - > $@; \
	fi

usermanual-ja.html: usermanual.txt
	if [ -n "$(ASCIIDOC)" ]; \
	then \
		cpp $< | sed -e 's/^#.*//' | $(ASCIIDOC) - > $@; \
	fi
